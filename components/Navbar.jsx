"use client"
import Link from "next/link";
import { usePathname } from "next/navigation";

const Navbar = () => {
    return (
        <nav>
            navigation
        </nav>
    )
    }

export default Navbar